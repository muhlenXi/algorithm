# 441

`Swift`

```swift
func arrangeCoins(_ n: Int) -> Int {
    var index = 1
    var sum = 0
    while index > 0 {
        sum += index
        if sum == n {
            return index
        } else if sum > n {
            return index - 1
        }
        index += 1
    }

    return 0
}

```